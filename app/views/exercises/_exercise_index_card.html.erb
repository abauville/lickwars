<%= link_to exercise_path(ex), class: "remove-link-style w-50" do %>
  <div class="teacher-exercise-card d-flex flex-column justify-content-center align-items-center mt-2">
    <p class="mb-0 lw-text-header"> <%= ex.name %></p>
  </div>
<% end %>
